; @privy-io/react-auth imports from @solana/kit, @solana-program/system and
; @solana-program/token as optional peers. They must be installed for the
; production bundle to link, but their own peer ranges disagree with the
; Kit v2 line Privy is built against, so npm's strict check rejects the tree.
legacy-peer-deps=true
